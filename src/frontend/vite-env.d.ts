/// <reference types="vite/client" />

// Gives TypeScript the ambient declarations for Vite's asset and CSS imports
// (`import logo from './logo.png'`, `import './theme.css'`), which the frontend
// relies on and which the Workers types alone do not provide.
